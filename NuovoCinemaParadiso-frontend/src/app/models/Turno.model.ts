export interface DtoTurno {
    id: string | null;
    oraInizio: string;
    oraFine: string;
    nome: string;
}

export interface DtoCreazioneTurno {
  oraInizio: string; 
  oraFine: string;  
  nome: string;
}
